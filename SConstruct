
SetOption("num_jobs", 6)

# include = "#export/_build/include"
# bin = "#export/_build/bin"
# lib = "#export/_build/lib"

# env = Environment(
#     CC="gcc",
#     CCFLAGS=[
#         "-Wall"
#     ],
#     CPPDEFINES=[],
#     CFlags=[
#         "-std=c99"
#     ],
#     CPPPATH=[include],
#     BINDIR=bin,
#     LIBDIR=lib,
#     LIBPATH=[lib],
#     LIBS=[]
# )

SConscript("utilities/SConscript")
